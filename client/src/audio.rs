pub struct AudioManager;

impl AudioManager {
    pub fn new() -> Self {
        // Init audio system
        Self
    }

    pub fn play_sound(&self, _id: &str) {
        // Play sound by ID
    }
}
