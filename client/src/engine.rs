pub struct Engine;

impl Engine {
    pub fn new() -> Self {
        // Initialize SDL, OpenGL, load assets, etc.
        Self
    }

    pub fn run(&mut self) {
        // Main game loop
        println!("Engine running!");
    }
}