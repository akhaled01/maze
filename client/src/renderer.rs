pub struct Renderer;

impl Renderer {
    pub fn new() -> Self {
        // Initialize SDL window, GL context, etc.
        Self
    }

    pub fn run(&mut self) {
        // Start the renderer main loop
    }
}
